QA Suite: SP1 Combined Bug Fixes (vgbo, 05lt, u29o, pyxj.4)
=============================================================

This QA suite specifies end-to-end test workflows that operate at the
user interface (CLI + output file inspection) without using internal
project APIs. All tests run via shell commands and inspect output files.

Prerequisites:
- uv environment synced (uv sync)
- Test fixtures available in tests/stpa/fixtures/
- A writable temp directory for output

--------------------------------------------------------------
Feature 1: Sanitize invalid ElementRefs in merge fallback (vgbo)
--------------------------------------------------------------

QA-VGBO-01: Pipeline does not crash when LLM produces invalid ElementRefs in Call 2
  Setup:
    - Create a use-case description file at tmp/qa_vgbo_use_case.txt
    - Create a mock LLM configuration that returns a ResponsibilitySet
      containing an invalid ElementRef (e.g. feedback_source with
      type=controlled_process, id=FB-1-1) in Call 2
    - Create a mock LLM configuration that returns an invalid
      ConnectionSet in Call 3 to trigger the merge failure path
  Action:
    - Run: uv run python -m pytest tests/stpa/test_merge_fallback_sanitize.py -x -k "sanitized"
  Expected output:
    - Exit code 0 (tests pass, no crash)
    - The test creates a ResponsibilitySet with invalid ElementRefs,
      triggers merge failure, and verifies the fallback produces a
      valid ControlStructure with nullified invalid refs

QA-VGBO-02: Sanitized fallback warnings are observable in output
  Setup:
    - Same as QA-VGBO-01
  Action:
    - Run the merge_with_fallback function through the test suite
    - Inspect the returned warnings list
  Expected output:
    - Warnings list contains entries mentioning stripped ElementRefs
    - Each warning identifies the element ID and the invalid reference

QA-VGBO-03: Further-degraded path strips ALL ElementRefs
  Setup:
    - Create a ResponsibilitySet that causes validation failure even
      after sanitization (e.g. duplicate resp_ids)
  Action:
    - Run the merge_with_fallback function through the test suite
  Expected output:
    - A ControlStructure is produced (no crash)
    - All feedback_source, control_action.target, and
      feedback_channel.source fields are None

QA-VGBO-04: Normal merge path unaffected
  Setup:
    - Create valid ResponsibilitySet and valid ConnectionSet
  Action:
    - Run the merge_with_fallback function through the test suite
  Expected output:
    - ControlStructure contains coordination links
    - Warnings list is empty
    - No sanitization occurs

--------------------------------------------------------------
Feature 2: RevisionDelta pattern for revision step (05lt)
--------------------------------------------------------------

QA-05LT-01: RevisionDelta schema is defined and used
  Setup:
    - Import the RevisionDelta model from the revision module
  Action:
    - Run: uv run python -c "from asago_scenario_generator.stpa.system_model.critic import RevisionDelta; print(RevisionDelta.model_fields.keys())"
  Expected output:
    - Output contains: new_responsibilities, new_controlled_processes,
      new_coordination_links, modified_responsibilities
    - Output does NOT contain: responsibilities (the full structure field)

QA-05LT-02: Revision produces valid ControlStructure via delta merge
  Setup:
    - Create a mock LLM that returns a RevisionDelta with a new
      responsibility RESP-3 (with valid PM, CA, FB elements)
    - Create a control structure with RESP-1 and RESP-2
    - Create CriticFindings with unjustified gaps
  Action:
    - Run: uv run pytest tests/stpa/test_revision_delta.py -x -k "merge"
  Expected output:
    - Exit code 0
    - Final control structure contains RESP-1, RESP-2, and RESP-3
    - Final control structure passes foundation validation

QA-05LT-03: revision_user.j2 contains numbered per-finding checklist
  Setup:
    - Load the template revision_user.j2 from the prompts directory
  Action:
    - Run: uv run python -c "
        from asago_scenario_generator.stpa.infra.templates import TemplateLoader
        from asago_scenario_generator.stpa.system_model._constants import PROMPTS_DIR
        loader = TemplateLoader(PROMPTS_DIR)
        text = loader.render_prompt('revision_user.j2', use_case_text='test', control_structure=None, critic_findings=None)
        assert 'You MUST add at least one element for EACH finding' in text
        print('OK')
      "
  Expected output:
    - Prints "OK"
    - The template text contains the numbered checklist instruction

QA-05LT-04: revision_system.j2 contains ID format rules
  Setup:
    - Load the template revision_system.j2
  Action:
    - Run: uv run python -c "
        from asago_scenario_generator.stpa.infra.templates import TemplateLoader
        from asago_scenario_generator.stpa.system_model._constants import PROMPTS_DIR
        loader = TemplateLoader(PROMPTS_DIR)
        text = loader.render_prompt('revision_system.j2', next_resp_num=3, next_cl_num=2)
        assert 'ID format rules' in text
        assert 'RESP-' in text
        assert 'CL-' in text
        print('OK')
      "
  Expected output:
    - Prints "OK"
    - Template contains ID format rules with next-available numbers

QA-05LT-05: revision_system.j2 next-available IDs are computed from existing structure
  Setup:
    - Create a control structure with RESP-1, RESP-2, and CL-1
  Action:
    - Render the revision_system.j2 template with computed next IDs
  Expected output:
    - next_resp_num = 3 (next after RESP-2)
    - next_cl_num = 2 (next after CL-1)

QA-05LT-06: strip_empty_responsibilities remains as safety net
  Setup:
    - Create a RevisionDelta with a new responsibility that has no
      PM parts, CAs, or FB channels
  Action:
    - Run: uv run pytest tests/stpa/test_revision_delta.py -x -k "strip"
  Expected output:
    - The empty responsibility is stripped
    - A warning is logged

--------------------------------------------------------------
Feature 3: Entry point category checklist in stage1b (u29o)
--------------------------------------------------------------

QA-U29O-01: stage1b_system.j2 contains all 5 entry point categories
  Setup:
    - Load the template stage1b_system.j2
  Action:
    - Run: uv run python -c "
        from asago_scenario_generator.stpa.infra.templates import TemplateLoader
        from asago_scenario_generator.stpa.system_model._constants import PROMPTS_DIR
        loader = TemplateLoader(PROMPTS_DIR)
        text = loader.render_prompt('stage1b_system.j2')
        for cat in ['User input surfaces', 'RAG/retrieval data sources', 'Tool execution results', 'External data feeds', 'Admin/config interfaces']:
            assert cat in text, f'Missing: {cat}'
        print('OK')
      "
  Expected output:
    - Prints "OK"
    - All 5 categories are present

QA-U29O-02: Each category has concrete examples
  Setup:
    - Load the template stage1b_system.j2
  Action:
    - Render the template and search for example text
  Expected output:
    - "chat interface" or "chat" appears near "User input surfaces"
    - "knowledge base" appears near "RAG/retrieval data sources"
    - "API call" or "API" appears near "Tool execution results"
    - "third-party" appears near "External data feeds"
    - "admin dashboard" or "admin" appears near "Admin/config interfaces"

QA-U29O-03: RAG category notes indirect ingress via poisoned content
  Setup:
    - Load the template stage1b_system.j2
  Action:
    - Render the template and search for indirect ingress text
  Expected output:
    - Text contains "indirect" near RAG category
    - Text contains "poison" or "poisoned" near RAG category

QA-U29O-04: Template notes component can appear in both tool_inventory and entry_points
  Setup:
    - Load the template stage1b_system.j2
  Action:
    - Render the template and search for dual-listing note
  Expected output:
    - Text mentions both "tool_inventory" and "entry_points"
    - Text clarifies that a component can appear in both

QA-U29O-05: Template preserves existing sections
  Setup:
    - Load the template stage1b_system.j2
  Action:
    - Render the template and verify existing sections
  Expected output:
    - "## Schneider zones" present
    - "## Rules" present
    - "## Emphasis" present
    - "## Quality requirements" present

--------------------------------------------------------------
Feature 4: Calls HTML report shows full content (pyxj.4)
--------------------------------------------------------------

QA-PYXJ-01: Call log entry includes full content fields
  Setup:
    - Create a call log entry with system_prompt, user_prompt, and
      response_content
  Action:
    - Run: uv run python -c "
        from asago_scenario_generator.stpa.infra.call_log import make_call_log_entry
        entry = make_call_log_entry(stage='stage_2', step='test', model='test-model',
            system_prompt='You are a safety engineer',
            user_prompt='Analyze this system',
            response_content='{\"result\": true}')
        assert 'system_prompt_text' in entry
        assert 'user_prompt_text' in entry
        assert 'response_content' in entry
        assert entry['system_prompt_text'] == 'You are a safety engineer'
        print('OK')
      "
  Expected output:
    - Prints "OK"
    - Entry contains system_prompt_text, user_prompt_text, response_content

QA-PYXJ-02: calls.jsonl entries contain full content when logged via log_llm_call
  Setup:
    - Create a run directory
    - Create an LLMResult with system_prompt, user_prompt, and content
  Action:
    - Call log_llm_call with the LLMResult
    - Read the calls.jsonl file
  Expected output:
    - JSONL entry contains system_prompt_text, user_prompt_text, response_content
    - The prompt text values match the LLMResult prompt values

QA-PYXJ-03: HTML report shows full prompt text in collapsible sections
  Setup:
    - Create a calls.jsonl file with entries containing system_prompt_text
      and user_prompt_text
  Action:
    - Run: uv run python -m asago_scenario_generator.stpa.infra.calls_html tmp/qa_calls.jsonl tmp/qa_calls.html
    - Read the output HTML file
  Expected output:
    - HTML file exists and is non-empty
    - HTML contains the system prompt text
    - HTML contains the user prompt text
    - HTML contains collapsible elements (details/summary or similar)

QA-PYXJ-04: HTML report pretty-prints structured JSON responses
  Setup:
    - Create a calls.jsonl file with an entry containing response_content
      as a JSON string
  Action:
    - Render to HTML
    - Read the output HTML
  Expected output:
    - HTML contains pretty-printed JSON with indentation
    - HTML contains a pre-formatted block

QA-PYXJ-05: HTML report includes search and filter box
  Setup:
    - Create a calls.jsonl file with multiple entries
  Action:
    - Render to HTML
    - Read the output HTML
  Expected output:
    - HTML contains an input element for search/filter
    - HTML contains inline JavaScript for filtering

QA-PYXJ-06: HTML report is self-contained
  Setup:
    - Create a calls.jsonl file with one entry
  Action:
    - Render to HTML
    - Read the output HTML
  Expected output:
    - HTML contains inline <style> tag
    - HTML contains inline <script> tag
    - HTML does not reference external stylesheets
    - HTML does not reference external scripts

QA-PYXJ-07: HTML report backward compatible with old entries
  Setup:
    - Create a calls.jsonl file with entries that do NOT contain
      system_prompt_text, user_prompt_text, or response_content fields
  Action:
    - Render to HTML
  Expected output:
    - HTML file is produced without errors
    - Summary table shows correct totals
    - Detail table shows existing metadata columns

QA-PYXJ-08: HTML report preserves summary table and metadata columns
  Setup:
    - Create a calls.jsonl file with 2 successful entries
  Action:
    - Render to HTML
    - Read the output HTML
  Expected output:
    - Summary shows total calls 2, success count 2
    - Detail table includes columns: stage, step, model, prompt_tokens,
      completion_tokens, duration_ms, timestamp

QA-PYXJ-09: CLI invocation produces HTML report
  Setup:
    - Create a calls.jsonl file
  Action:
    - Run: uv run python -m asago_scenario_generator.stpa.infra.calls_html <calls.jsonl> <output.html>
  Expected output:
    - Exit code 0
    - Output message: "HTML report written to <output.html>"
    - HTML file exists at the specified path
