## Control Structure

{{ control_structure_yaml }}

## Hazards and Security Constraints

{{ loss_analysis_yaml }}

## Technology Context

{{ technology_context }}

---

## Your Task

Fill the following ICA slots for responsibility **{{ resp_id }}**. For each slot, either provide concrete ICAs (Insecure Control Actions) or declare it N/A with a structural justification. Follow the ICA type definitions and requirements in the system prompt.

### Slots to fill:

{{ slots_yaml }}

### Reference guidance:

- Each ICA must reference valid hazard IDs (H-*) and constraint IDs (SC-*) from the loss analysis above. Use `related_hazards` for H-* and `related_constraints` for SC-*.
- **ID mapping note:** The control structure's `security_constraint_refs` may use REQ-* from requirements derivation; use SC-* from the loss analysis above for `related_constraints`.
- Reference process model parts (PM-*) and feedback channels (FB-*) from the control structure where relevant.
- Use the technology context to inform AI-specific failure modes.
- Each slot's `uca_type` (NOT_PROVIDED, INCORRECT, WRONG_TIMING, WRONG_DURATION) is defined in the input slot schema.

Return JSON with a `filled_slots` array matching the slot schema with `is_na`, `icas`, and `na_justification` filled in.
