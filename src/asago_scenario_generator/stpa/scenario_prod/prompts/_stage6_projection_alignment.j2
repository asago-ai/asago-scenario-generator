{% if projection_alignment is defined and projection_alignment %}
## Projection Alignment

{{ projection_alignment }}

Realize the projection rows strictly in table order: each listed factor before the next, and the unsafe control action row last. Do not invent any causal factor, temporal assertion, or scenario step.

{% endif %}
